{#- Managed by ansible (role: dev_worker, ADR 0021). See tep-kubeconfig.ctmpl.j2 for the contract.
    This one carries the helmtest-dw<N> credential — the namespace an agent may actually `helm
    install` into. -#}
{% raw %}{{ with secret "af/data/dev-workers/{% endraw %}{{ inventory_hostname }}{% raw %}" }}{{ .Data.data.helmtest_kubeconfig }}{{ end }}{% endraw %}
