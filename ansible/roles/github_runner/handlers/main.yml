---
- name: Restart docker
  ansible.builtin.systemd:
    name: docker
    state: restarted

- name: Restart github-runner
  ansible.builtin.systemd:
    name: "{{ github_runner_service }}"
    state: restarted
    daemon_reload: true
