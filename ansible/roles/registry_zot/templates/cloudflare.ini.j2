# Managed by Ansible (registry_zot). Cloudflare API token for certbot DNS-01.
# Needs Zone:DNS:Edit on chifor.me (same scoped token the cloudflare tofu module uses).
dns_cloudflare_api_token = {{ cloudflare_dns_api_token }}
