apiVersion: v1
kind: Namespace
metadata:
  name: vaultwarden
  labels:
    # Vaultwarden binds :80 (root, no caps); baseline is sufficient (like gitea).
    pod-security.kubernetes.io/enforce: baseline
    pod-security.kubernetes.io/warn: baseline
