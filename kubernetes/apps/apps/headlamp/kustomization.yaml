apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Headlamp — live Kubernetes cluster explorer (read-only) at k8s.chifor.me, gated by Cloudflare Access.
resources:
  - namespace.yaml
  - rbac.yaml
  - rbac-flux.yaml # scoped Flux-CR safe-ops write (patch) for the Headlamp SA — see the file header
  - headlamp.yaml
  - networkpolicy.yaml # ingress only from the cloudflared tunnel (no in-cluster Access bypass)
