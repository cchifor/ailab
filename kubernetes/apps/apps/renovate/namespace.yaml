apiVersion: v1
kind: Namespace
metadata:
  name: renovate
  labels:
    # renovate/renovate runs non-root (uid 12021); restricted-compatible.
    pod-security.kubernetes.io/enforce: restricted
    pod-security.kubernetes.io/warn: restricted
