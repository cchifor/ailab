apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - renovate-secret.sops.yaml # SOPS: fine-grained GitHub PAT
  - config-configmap.yaml # bot config.js (which repo + auth + behavior)
  - cronjob.yaml # runs every 4h
