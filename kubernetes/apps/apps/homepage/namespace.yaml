apiVersion: v1
kind: Namespace
metadata:
  name: homepage
  labels:
    app.kubernetes.io/part-of: homepage
    # Homepage runs non-root, no caps, no host access -> restricted is safe.
    pod-security.kubernetes.io/enforce: restricted
