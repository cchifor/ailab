apiVersion: v1
kind: LimitRange
metadata:
  name: default-limits
  namespace: homepage
spec:
  limits:
    - type: Container
      default: { cpu: 250m, memory: 256Mi }
      defaultRequest: { cpu: 50m, memory: 64Mi }
