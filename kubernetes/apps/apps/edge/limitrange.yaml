# Default-injection safety net for the edge namespace (see monitoring/limitrange.yaml for rationale).
apiVersion: v1
kind: LimitRange
metadata:
  name: default-limits
  namespace: edge
spec:
  limits:
    - type: Container
      default: { cpu: 500m, memory: 512Mi }
      defaultRequest: { cpu: 50m, memory: 128Mi }
