apiVersion: v1
kind: LimitRange
metadata:
  name: default-limits
  namespace: auth
spec:
  limits:
    - type: Container
      default: { cpu: 500m, memory: 256Mi }
      defaultRequest: { cpu: 25m, memory: 64Mi }
