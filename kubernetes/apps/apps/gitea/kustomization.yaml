apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - gitea-admin.sops.yaml # SOPS: break-glass local admin (username/password)
  - gitea-oauth.sops.yaml # SOPS: Authelia OIDC client (key/secret)
  # - gitea-actions-s3.sops.yaml # FOLLOW-UP: versitygw S3 creds for Actions artifacts/logs (ADR 0017).
  #   Add back (create the encrypted file from .example) once the gitea-actions bucket + scoped key exist.
  - gitea.yaml # HelmRepository + HelmRelease (SQLite on qnap-iscsi, OIDC login, Actions enabled)
