apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: alloy
  namespace: monitoring
spec:
  interval: 1h
  chart:
    spec:
      chart: alloy
      version: "1.10.0"
      sourceRef:
        kind: HelmRepository
        name: grafana
        namespace: flux-system
  values:
    controller:
      type: daemonset
      priorityClassName: platform-critical # node log collector — survive pressure
    alloy:
      resources:
        # 256Mi -> 512Mi: the cp2 replica repeatedly OOMKilled (exit 137) at 256Mi — its working set
        # (tailing /var/log/pods + Loki WAL/batching) exceeds 256Mi under a chattier log source, dropping
        # log shipping on each restart. 512Mi gives headroom; request bumped 128->192Mi. (2026-07-22)
        requests: { cpu: 50m, memory: 192Mi }
        limits: { memory: 512Mi }
      # mount /var/log so we can read node pod-log files directly (no API-tailing rate limits)
      mounts:
        varlog: true
      configMap:
        content: |
          discovery.kubernetes "pods" {
            role = "pod"
          }
          discovery.relabel "pod_logs" {
            targets = discovery.kubernetes.pods.targets
            rule {
              source_labels = ["__meta_kubernetes_namespace"]
              target_label  = "namespace"
            }
            rule {
              source_labels = ["__meta_kubernetes_pod_name"]
              target_label  = "pod"
            }
            rule {
              source_labels = ["__meta_kubernetes_pod_container_name"]
              target_label  = "container"
            }
            rule {
              source_labels = ["__meta_kubernetes_pod_node_name"]
              target_label  = "node"
            }
            rule {
              source_labels = ["__meta_kubernetes_pod_label_app_kubernetes_io_name"]
              target_label  = "app"
            }
            // map each container to its on-node log file: /var/log/pods/<ns>_<pod>_<uid>/<container>/*.log
            rule {
              source_labels = ["__meta_kubernetes_pod_uid", "__meta_kubernetes_pod_container_name"]
              separator     = "/"
              action        = "replace"
              replacement   = "/var/log/pods/*$1/*.log"
              target_label  = "__path__"
            }
          }
          local.file_match "pod_logs" {
            path_targets = discovery.relabel.pod_logs.output
          }
          loki.source.file "pod_logs" {
            targets    = local.file_match.pod_logs.targets
            forward_to = [loki.process.pod_logs.receiver]
          }
          loki.process "pod_logs" {
            stage.cri {}
            // Drop Alloy's benign one-time-per-startup remotecfg error (remotecfg is unconfigured
            // by design -> err="noop client"); it's cosmetic and otherwise pollutes error/warn views.
            stage.drop {
              expression          = "remotecfg.+noop client"
              drop_counter_reason = "alloy_remotecfg_noop"
            }
            forward_to = [loki.write.default.receiver]
          }
          loki.write "default" {
            endpoint {
              url = "http://loki.monitoring.svc.cluster.local:3100/loki/api/v1/push"
            }
          }
