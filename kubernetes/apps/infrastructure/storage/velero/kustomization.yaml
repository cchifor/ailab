apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - secret.sops.yaml # SOPS: versitygw creds for the `velero` bucket (key `cloud`)
  - helmrelease.yaml # Velero chart 12.0.3 (CSI data movement -> versitygw) + daily/weekly schedules
