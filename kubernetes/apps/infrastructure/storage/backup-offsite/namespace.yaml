apiVersion: v1
kind: Namespace
metadata:
  name: backup-offsite
  labels:
    # rclone runs non-root with a read-only config + emptyDir cache — baseline is sufficient.
    pod-security.kubernetes.io/enforce: baseline
    pod-security.kubernetes.io/warn: baseline
