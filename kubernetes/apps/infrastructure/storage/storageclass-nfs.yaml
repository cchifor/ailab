apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: nfs-csi
  annotations:
    # Default for now (RWX). Flip to the QNAP iSCSI class once that's added (RWO block + snapshots).
    storageclass.kubernetes.io/is-default-class: "true"
provisioner: nfs.csi.k8s.io
parameters:
  server: 10.55.0.254 # QNAP storage-fabric IP — reached via the host router+SNAT over Thunderbolt (WS2)
  share: /pve-nfs
  mountPermissions: "0777"
reclaimPolicy: Delete
volumeBindingMode: Immediate
allowVolumeExpansion: true
mountOptions:
  - nfsvers=4.0 # QuTS hero NFS rejects 4.1 ("Protocol not supported"); 4.0 is single-port + NAT-friendly
  - hard
  - nconnect=4
