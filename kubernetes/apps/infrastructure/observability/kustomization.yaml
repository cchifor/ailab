apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - limitrange.yaml
  - alloy-otlp.yaml # OTLP collector for the strive platform (metrics → prometheus, sampled traces → tempo)
  - tempo.yaml # trace store (grafana/tempo single-binary) + ingest/query NetworkPolicy
