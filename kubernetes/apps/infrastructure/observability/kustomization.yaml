apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - limitrange.yaml
  - alloy-otlp.yaml # OTLP metrics collector for the strive platform (stage A: metrics only)
