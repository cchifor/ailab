apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Cluster-wide core primitives that must exist before any workload references them
# (wired FIRST in ../kustomization.yaml). Currently: the platform PriorityClasses.
resources:
  - priority-classes.yaml
