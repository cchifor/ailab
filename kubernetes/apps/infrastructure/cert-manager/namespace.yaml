apiVersion: v1
kind: Namespace
metadata:
  name: cert-manager
  labels:
    # cert-manager components run non-root; baseline is sufficient (webhook/cainjector don't need more).
    pod-security.kubernetes.io/enforce: baseline
    pod-security.kubernetes.io/warn: baseline
