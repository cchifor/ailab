apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: trueswarm
  namespace: flux-system
spec:
  interval: 1m
  url: ssh://git@gitea-ssh.gitea.svc.cluster.local:2222/cchifor/trueswarm.git
  ref:
    branch: main
  secretRef:
    name: trueswarm-deploy-key
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: trueswarm
  namespace: flux-system
spec:
  interval: 5m
  retryInterval: 1m
  timeout: 15m
  sourceRef:
    kind: GitRepository
    name: trueswarm-admin
  dependsOn:
  - name: trueswarm-admin-foundation
  - name: trueswarm-admin-security
  - name: trueswarm-platform-owner-rotation
  path: ./deploy/platform
  prune: true
  wait: true
  decryption:
    provider: sops
    secretRef:
      name: sops-age
