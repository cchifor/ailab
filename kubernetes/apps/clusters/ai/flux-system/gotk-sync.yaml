# This manifest was generated by flux. DO NOT EDIT.
# Repointed to the in-cluster Gitea forge (ADR 0017 cutover). GitHub's copy keeps the ssh://github URL
# as a rollback: patch the live GitRepository back to ssh://git@github.com/cchifor/ailab + secret flux-system.
---
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 1m0s
  ref:
    branch: main
  secretRef:
    name: flux-gitea-auth
  url: http://gitea-http.gitea.svc.cluster.local:3000/cchifor/ailab.git
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./kubernetes/apps/clusters/ai
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
